#include<stdio.h>
int main()
{
    int num;
    printf("Enter the number: ");
    scanf("%d",&num);

    if(num%num=0 && num%1=0)
    {printf("The number is prime");
    }
    else{
        printf("the number is coprime");
    }
    return 0;
}